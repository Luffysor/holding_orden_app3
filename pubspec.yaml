name: holding_orden_app
description: Ordenes de material Holding Group, almacenadas localmente.
publish_to: none
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  intl: ^0.19.0
  path: ^1.9.0
  pdf: ^3.11.1
  printing: ^5.13.4
  signature: ^5.5.0
  sqflite: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
